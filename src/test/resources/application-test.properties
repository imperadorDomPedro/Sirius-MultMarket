# origem permitida nos testes deve bater com o preflight dos testes CORS
cors.allowed-origins=http://localhost:3000,http://localhost:5173

# ?? Banco em mem�ria ? substitui PostgreSQL ??????????????????????????????????
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;MODE=PostgreSQL
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop

# ?? Flyway desabilitado ? H2 cria o schema via ddl-auto ?????????????????????
spring.flyway.enabled=false

# ?? JWT dummy ????????????????????????????????????????????????????????????????
jwt.secret=guia-gurupi-secret-key-muito-segura-2024
jwt.expiration=86400000

# ── MinIO (desabilita para testes) ────────────────────────────────────────
minio.endpoint=http://localhost:9000
minio.access-key=minioadmin
minio.secret-key=minioadmin
minio.bucket-name=guiadegurupi